Help to setup the envirement on your machine

The code is test and run using package management with anaconda:
https://www.continuum.io/anaconda-overview

If you installed anacondo, you can use the .yml file to install the version we used. The .sh is a small macro that does the installation and envirement definition used for testing deepJet (on the lapops).
 
